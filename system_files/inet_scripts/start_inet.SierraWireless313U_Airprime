#!/bin/bash

# Starts the Sierra Wireless AirCard 313U Airprime model cell modem using the
# UMTSkeeper script.  Run "lsusb" after the usb-modeswitch occurs to see if
# the "USBMODEM" parameter matches that is the command below.  Also, this script
# assumes that "Airprime" occurs in the "lsusb" output.
# See documentation of the UMTSkeeper script at:
# http://mintakaconciencia.net/squares/umtskeeper/

/home/pi/umtskeeper/umtskeeper --sakisoperators "USBINTERFACE='3' OTHER='USBMODEM' USBMODEM='0f3d:68aa' APN='CUSTOM_APN' CUSTOM_APN='web.gci' APN_USER='0' APN_PASS='0'" --sakisswitches "--sudo --console" --devicename 'Airprime' --statfilepath /var/log --nostats --log
